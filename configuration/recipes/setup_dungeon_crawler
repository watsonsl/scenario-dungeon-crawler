script "setup_dungeon_crawler" do
    interpreter "bash"
    user "root"
    cwd "/tmp"
    code <<-EOH
    cd /root
    git clone https://github.com/watsonsl/scenario-dungeon-crawler.git
    cd scenario-dungeon-crawler
    ./setup
    cd /root
    rm -r scenario-dungeon-crawler
    EOH
end
